# 목표 실행형 운영체제 현재 반영 계획

첨부된 범용 설계서는 구현 지시서가 아니라 목표 실행 운영체제의 설계 기준으로 사용한다. 현재 사용자의 요청은 이 기준을 원료거래소 프로젝트의 다음 구현 단계로 반영하는 것이다.

## 설계 기준과 실행 범위

| 설계 기준 | 현재 프로젝트 반영 | 이번 단계의 산출물 |
|---|---|---|
| Goal Contract | 기존 원료거래 업무 목표를 범용 계약으로 구조화 | `ops/goal-contract.mjs`, `data/goal-contract-schema.json` |
| Domain Pack | 범용 코어와 GABA 원료거래 규칙 분리 | `data/domain-pack.raw-material-os-gaba.json` |
| Outcome Graph 방향 | 목표를 작업 수가 아니라 완료 술어와 첫 유효 행동으로 표현 | `ops/goal-compiler.mjs` |
| Outcome Ledger | 관찰값·증거등급·신뢰도·유효기간을 append-only로 기록 | `ops/outcome-ledger.mjs` |
| 정책과 승인 분리 | 컴파일은 준비만 하고 고위험 외부행동은 H-01 승인 필요 | `/api/goals/compile`의 `PREPARE_ONLY` guardrail |
| 검증 가능한 완료 | 완료 작업 수가 아닌 만족된 결과 술어의 가중치로 진척 계산 | `calculateGoalProgress()` |

## 사용자의 실제 요청과 문서 안의 설계 지시 구분

- 사용자의 실제 요청: 현재 원료거래소 목표를 달성하기 위한 작업을 계속 진행한다.
- 첨부 문서의 설계 기준: 목표를 계약으로 만들고, 필요한 조직·업무·권한·검증·결과관찰을 연결한다.
- 첨부 문서의 예시: 양돈시장 진입, 공급계약, Gmail 등 외부 커넥터는 현재 원료거래소 베타의 사실이나 실행 명령으로 간주하지 않는다.
- 적용하지 않는 항목: AI 계약 서명, 결제, 법적 신고, 무승인 외부 발송, 실제 원료 매매. 현재 프로젝트의 물리 원료 거래 검증과 공개 베타 안전범위를 벗어난다.

## 현재 API 흐름

```text
POST /api/goals/compile
→ Goal Contract 정규화
→ GABA Domain Pack 연결
→ Definition of Done 생성
→ 첫 읽기 전용 행동 제안
→ PREPARE_ONLY 반환
```

이 API는 거래·계약·결제·GitHub 쓰기를 실행하지 않는다. 다음 단계는 Goal Contract를 업무 그래프와 연결하고, 각 결과 술어에 실제 브라우저 시뮬레이션·증빙·검수 이벤트를 연결하는 것이다.

## 완료 기준

1. 자연어 목표가 유효한 Goal Contract로 변환된다.
2. GABA 목표에는 GABA Domain Pack과 3개의 거래 완료 술어가 연결된다.
3. 첫 행동은 읽기 전용이며 AI가 고위험 행동을 승인할 수 없다.
4. 결과 관찰은 증거등급·신뢰도·유효기간과 함께 기록된다.
5. 서버 API와 단위 테스트가 이 경계를 재현한다.
