# 실물 원료 거래 플랫폼 BM 특허 선행기술 조사 의뢰 패킷

버전: v0.1  
작성일: 2026-09-08  
대상: GABA 단일 원료 폐쇄형 MVP에서 모든 실물 원료로 확장 가능한 Raw Material OS

> 본 패킷은 특허 등록 가능성을 보장하는 법률의견이 아니다. 구현된 기술구성과 발명 후보를 변리사에게 전달하고, 선행기술 조사·청구항 설계·진보성 검토를 요청하기 위한 내부 자료다.

## 1. 발명 후보 명칭

**증빙 생명주기와 표준 스펙을 이용한 실물 원료 거래 체결 시스템 및 방법**

보조 명칭 후보:

- 거래 전 증빙·로트 상태 재검증과 원자적 재고 잠금을 수행하는 실물 원료 거래 시스템
- 자연어 원료 요구와 증빙 잠금형 공급 로트를 연결하는 B2B 원료 거래 운영 방법

## 2. 해결하려는 기술적 문제

일반적인 B2B 마켓플레이스는 상품명·가격·수량을 연결하는 데 그쳐 다음 문제를 해결하지 못한다.

1. 같은 원료명이 서로 다른 순도·제형·원산지·포장·용도를 가리킨다.
2. 매물 등록 당시 유효했던 COA·규격서·재고가 체결 시점에는 변경될 수 있다.
3. 동일 로트가 여러 구매자에게 중복 예약되거나 실제 재고보다 많이 판매될 수 있다.
4. 납품 후 품질 불일치와 거래 전 증빙 불일치가 동일한 오류로 기록되면 책임·분쟁 추적이 어렵다.
5. 양측에 같은 거래 상태를 보여주면서도 원가·고객정보 같은 민감정보는 최소 공개해야 한다.

## 3. 제안 기술의 구성요소

### E1. Material Master와 표준 Spec 식별

사용자가 `GABA`, `가바` 등 동의어를 입력하면 Material ID로 정규화하고, 순도·제형·원산지·포장·사용목적·납품조건을 Spec ID에 연결한다. 근거 문서가 없는 기본값은 `PENDING_HUMAN_APPROVAL`로 표시한다.

### E2. Evidence·Lot·Inventory 결합

COA·SDS·TDS·로트 추적성·재고증빙을 실제 공급 로트에 연결한다. 증빙의 유효기간과 상태를 매물의 거래 가능성 계산에 포함한다.

### E3. 거래 전 노출 게이트

다음 조건을 동시에 만족하는 로트만 구매자에게 노출한다.

```text
Spec 일치 ∧ 증빙 유효 ∧ Lot 추적 가능 ∧ 가용재고 충분 ∧ 납기 가능 ∧ 가격 조건 충족
```

### E4. 체결 직전 재검증과 원자적 예약

구매 주문과 공급자 수락 요청에 멱등 키를 결속하고, 공급자가 주문을 수락하는 순간 E3 조건을 다시 계산한다. 로트별 트랜잭션 잠금과 예약 ID를 이용해 동일 요청은 기존 결과를 반환하고 재고를 한 번만 차감한다. 검증 실패 또는 재고 부족이면 체결 원장을 생성하지 않는다.

### E5. 불변 체결 스냅샷

체결 시점의 Spec·Lot·Evidence·가격·수량·납기·검증 결과·예약 ID를 Trade에 저장하고, 해당 스냅샷의 정규화 데이터에 SHA-256 무결성 지문(`tradeSnapshotHash`)을 봉인한다. 이후 원본 문서나 재고가 변해도 체결 당시 근거를 재현하고, 납품·검수 시 스냅샷 변조 여부를 재검증할 수 있다.

### E6. 납품 후 이행 상태 분리

납품 후 품질 불일치는 거래 전 게이트 실패로 소급하지 않고 `DELIVERED → INSPECTED/DISPUTED`로 기록한다. 불일치 로트는 `QUARANTINED` 상태로 전환한다.

### E7. 가격값 산출 조건과 출처 관리

완료·증빙 유효 거래만 가격지표 표본으로 사용한다. 최소 체결 수 또는 최소 공급자 수에 미달하면 가격값을 만들지 않고 `UNAVAILABLE`을 반환한다.

## 4. 데이터 흐름

```text
자연어 원료명·구매요구
  → Material ID / Spec ID
  → Evidence·Lot·Inventory 상태 계산
  → 검증 통과 Offer만 노출
  → Buyer Order
  → Supplier 수락 시 재검증
  → Idempotent Atomic Reservation
  → Trade Snapshot
  → Delivery / Inspection / Dispute / Settlement
```

## 5. 기술적 효과 후보

- 원료 동의어와 거래조건 혼동에 따른 오주문을 줄인다.
- 문서 만료·재고 불일치·스펙 불일치 매물의 체결 전 노출을 차단한다.
- 멱등 예약과 원자적 재고 차감으로 중복체결·초과판매를 방지한다.
- 체결 당시 근거와 납품 후 품질판정을 분리해 책임관계를 재현한다.
- 가격 표본과 출처가 부족할 때 허위 정밀도를 만들지 않는다.
- 거래 가능성 판단에 필요한 정보만 공유해 양측의 민감정보 노출을 줄인다.

## 6. 단순 기능과 구별할 핵심

다음 요소만으로는 발명의 중심을 구성하지 않는다.

- 검색창 또는 원료명 동의어 처리만
- 가격·수량 입력과 수락·거절만
- 주식창 형태의 호가·차트만
- AI가 상승·하락 문구를 출력하는 것만

조사의 중심은 **표준 Spec과 증빙 생명주기, 로트별 상태 계산, 체결 직전 재검증, 멱등 원자적 예약, 체결 스냅샷, 납품 후 분쟁 상태 분리의 결합**으로 둔다.

## 7. 청구항 설계 초안 방향

### 독립항 후보

표준 원료 식별자와 거래 스펙을 생성하는 단계;
공급 로트에 증빙과 가용재고 상태를 연결하는 단계;
증빙 5종이 같은 로트·조직에 연결되고 인간 검토를 통과한 경우에만 로트를 생성하며, 스펙 적합성·증빙 유효성·로트 추적성·재고·납기 조건을 계산해 매물을 선택적으로 노출하는 단계;
구매 주문 수락 시 상기 조건을 재검증하고 예약 식별자에 따라 로트를 원자적으로 예약하는 단계;
체결 당시 스펙·로트·증빙·가격·수량을 스냅샷으로 저장하는 단계;
증빙 또는 이행 상태 변경에 따라 매물 또는 로트를 격리하는 단계를 포함하는 실물 원료 거래 방법.

### 종속항 후보

- 자연어 동의어·누락 필드·모순 필드에 따른 추가 질문 순서
- 예약 식별자 기반 중복 요청 재사용
- 최소 표본·공급자 다양성 미달 시 가격지표 산출 불가 처리
- 납품 후 품질 불일치의 `DISPUTED`·`QUARANTINED` 전환
- 구매자·공급자 화면의 공통 상태와 권한별 최소 정보 공개

## 8. 선행기술 조사 질문

변리사에게 다음 조합으로 조사 요청한다.

1. B2B 원료·화학물질·식품소재의 로트·COA·재고 연동 거래 시스템
2. 전자상거래의 재고 예약·멱등성·중복 주문 방지
3. 문서 유효기간 변경에 따른 상품 노출·거래 상태 자동 격리
4. 자연어 상품 요구를 구조화된 사양 ID로 변환하는 시스템
5. 공급자 증빙 신뢰도와 완료 거래를 이용한 가격지수
6. 납품 후 품질 클레임과 체결 전 검증을 분리하는 이행 시스템

검색어 예시:

```text
"raw material" lot COA inventory reservation marketplace
"certificate of analysis" inventory verified offer transaction
evidence lifecycle lot quarantine B2B material trading
specification normalization natural language procurement
idempotent atomic inventory reservation purchase order
납품 원료 로트 COA 재고 예약 거래 시스템
증빙 유효기간 매물 격리 원료 거래
```

## 9. 현재 구현 증거

| 구성 | 구현·검증 파일 |
|---|---|
| Material Master·동의어 | `data/material-master.json`, `beta-app/material-master.mjs` |
| 로트 등록 전 증빙 게이트 | `beta-app/evidence-registry.mjs`, `beta-app/test-lot-onboarding.mjs` |
| Evidence-Locked Inventory | `beta-app/inventory-ledger.mjs` |
| 체결·멱등성·스냅샷·무결성 지문 | `beta-app/trade-engine.mjs`, `beta-app/snapshot-integrity.mjs`, `beta-app/test-snapshot-integrity.mjs` |
| 가격지표 정책 | `data/price-index-policy.json`, `beta-app/price-index.mjs` |
| Shadow Pilot | `ops/shadow-pilot-plan.json` |
| 자동 품질 게이트 | `.github/workflows/quality-gates.yml` |

## 10. 반드시 확인받을 사항

- 국내·해외 선행기술과의 차이 및 진보성
- 상태머신·원자적 예약이 특허상 기술적 수단으로 충분히 구체적인지
- 가격지표와 자연어 스펙 변환을 하나의 발명으로 묶을지 분할 출원할지
- 자본시장형 상품으로 오인되지 않도록 실물 구매·공급 범위를 청구항에 어떻게 한정할지
- 공개 베타 이전 출원 시점과 공개자료 관리

## 11. 결론

현재 단계의 가장 강한 후보는 단순한 원료 마켓플레이스가 아니라, **증빙이 살아 있고 재고가 잠겨 있는 경우에만 실물 거래를 허용하며, 체결 당시의 근거와 납품 후 이행 상태를 재현하는 운영 시스템**이다. 특허 출원 여부와 청구항은 선행기술 조사 및 변리사 검토 후 결정한다.
