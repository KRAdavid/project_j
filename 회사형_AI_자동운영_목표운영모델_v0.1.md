# 회사형 AI 자동운영 목표 운영모델

버전: v0.1  
작성일: 2026-09-08  
대상: GABA 폐쇄형 베타에서 전체 실물 원료로 확장하는 Raw Material OS

## 1. 결론

AI 팀이 회사처럼 움직이려면 AI에게 일을 시키는 것만으로는 부족하다. 다음 다섯 가지를 시스템의 기본 규칙으로 고정해야 한다.

```text
목표·책임자 → 업무 큐 → 자동 실행 → 증거·검토 → 승인·실행 → 측정·개선
```

핵심은 **AI의 자율성은 분석·생성·검증까지 넓히고, 거래·계약·지급·분쟁 종결의 책임 권한은 H-01에게 남기는 것**이다.

## 2. 회사형 운영의 최소 구조

| 층 | 역할 | 현재 기준 |
|---|---|---|
| 목표층 | 북극성, 분기 목표, 중단 기준 | 검증된 실물 원료의 정확하고 신속한 체결 |
| 업무층 | 업무 카드, 우선순위, 의존성, 기한 | `ops/task-queue.json` |
| 실행층 | AI 역할별 분석·설계·코드·테스트·업무 오케스트레이션 | AI-01~AI-13 |
| 통제층 | 권한, 증거, 감사, 승인, 격리 | H-01 승인 게이트 + fail-closed |
| 운영층 | 모니터링, 장애 대응, 백업, 복구 | health·SSE·readiness·운영 런북 |
| 학습층 | KPI, 사용자 피드백, 실패 회고, 규칙 개선 | 일일 리포트·주간 의사결정 |

## 3. 자동으로 돌아가는 업무 생명주기

### 3.1 업무 생성

다음 사건은 사람이 티켓을 만들지 않아도 자동으로 업무 카드를 만든다.

- 문서 만료 임박, COA·Spec 불일치, 로트 재고 감소
- 중복 예약·음수 재고·권한 오류·SSE 단절
- 가격 원천 결측·표본 부족·승인 만료
- 주문 응답 지연·반복 문의·스펙 수정 반복
- 테스트 실패·배포 실패·백업 리허설 미실시

자동 생성된 업무에는 목적, 위험도, 담당 AI, 검토 AI, 완료 정의, 증거 경로, 인간 게이트를 반드시 채운다.

### 3.2 자동 실행

오케스트레이터가 업무 큐에서 다음 업무를 선택하고, 담당 AI와 검토 AI를 병렬 실행한다. 자동 사이클은 `queued` 상태의 준비 업무만 `working`으로 인수하고 담당자·시각을 기록한다. 이미 `working` 또는 `review`인 업무를 반복 인수하지 않는다. 결과는 코드 변경이 아니라 다음 다섯 가지로 정규화한다.

1. 확인된 사실
2. 가정과 불확실성
3. 발견한 위험
4. 제안 조치
5. H-01 결정 필요 항목

검토 AI가 서로 다른 결론을 내리면 자동 병합하지 않고 `CONFLICT_REVIEW`로 올린다.

### 3.3 승인과 실행

다음 상태만 자동 실행 가능하다.

- 문법 검사, 단위·통합 테스트, 시뮬레이션
- 문서 초안, 분석 리포트, 사용자 질문 개선안
- 매물 재검증, 만료 매물 숨김, 위험 로트 격리
- 장애 알림, 읽기 전용 전환, 대체 공급자 탐색

다음은 H-01의 명시적 승인 없이는 완료 처리하지 않는다.

- 실제 주문 체결, 공급계약 확정, 가격지표 공개
- 결제·지급·환불·수수료 확정
- 품질 적합 판정 및 분쟁 종결
- 운영 환경 배포와 PostgreSQL·Object Storage·인증 연결

## 4. 자동 운영 제어탑

운영 제어탑은 다음 화면과 API를 단일 기준으로 제공해야 한다.

| 화면/큐 | 보여줄 것 | 행동 |
|---|---|---|
| 승인 대기 | 결정, 위험, 근거, 영향 범위 | 승인·수정요청·보류·거절 |
| 거래 안전 | 노출 로트, 증빙, 재고, 예약 충돌 | 노출 중지·격리·재검증 |
| 운영 건강 | API, DB, SSE, 저장소, 백업, 인증 | 읽기 전용·거래중지·복구 시작 |
| 품질 회고 | 실패 테스트, 클레임, 재발 패턴 | 규칙 수정 업무 생성 |
| 특허 증거 | 구성요소, 코드, 테스트, 차별점 | 변리사 검토 패킷 갱신 |

모든 결정은 `decision_id`, 결정자, 시각, 대상 버전, 근거, 만료시각을 가진 변경 불가 감사기록으로 남긴다.

## 5. 원료 거래에 필요한 불변 통제

자동 운영의 중심은 AI가 아니라 원장이다.

```text
Material Master
 → Spec
 → Evidence
 → Lot / Inventory
 → Offer
 → Order
 → Atomic Reservation
 → Trade Snapshot
 → Delivery / Inspection / Dispute
```

- 거래 가능 매물은 유효 증빙·추적 가능한 로트·가용재고·납기를 모두 통과해야 한다.
- 공급자 수락 순간 조건을 재검증하고, 멱등 예약으로 중복·초과 체결을 막는다.
- 체결 스냅샷과 무결성 지문은 이후 변경과 분리해 보존한다.
- 납품 후 불일치는 거래 전 검증 실패로 소급하지 않고 `DISPUTED`·`QUARANTINED`로 분리한다.
- 가격·추세는 허용 출처, 최소 표본, 공급자 다양성, 인간 공개 승인을 모두 통과하기 전에는 공개하지 않는다.

## 6. 회사 운영 주기

### 매일 자동 운영

1. 업무 큐와 실패·보류 업무를 읽는다.
2. 증빙 만료, 재고, 예약, 권한, SSE, 백업 상태를 점검한다.
3. 전체 테스트와 보안·회귀 검사를 실행한다.
4. 위험도 순으로 승인 대기 패킷을 만든다.
5. 대기 중인 준비 업무가 있으면 자동 인수하고, 실행 중 업무는 후속 검토 패킷으로 이어간다.
6. 변화가 없으면 조용히 기록하고, 의미 있는 변화만 H-01에게 알린다.

### 매주 H-01 의사결정

다음 네 가지 중 하나만 선택한다.

`계속` · `수정` · `보류` · `중단`

판단 기준은 스펙 확정률, 검증 매물 비율, 주문 응답시간, 체결 후 분쟁률, 자동 차단 정확도, 장애 복구시간, 미해결 고위험 업무 수다.

## 7. 예외와 장애 처리

예외는 AI가 조용히 보정하지 않는다.

```text
탐지 → 영향범위 계산 → 신규 위험 거래 차단
→ 증거 보존 → 담당 AI 조사 → H-01 판단
→ 복구·재검증 → 재발방지 업무 생성
```

다음 상황에서는 즉시 신규 체결을 중지하고 읽기 전용으로 전환한다.

- 원장·감사로그·체결 스냅샷을 재현할 수 없음
- DB·증빙 저장소·인증 공급자 상태가 불명확함
- 재고·증빙·권한의 원천이 서로 충돌함
- 중복 예약, 위조 토큰, 대량 오류가 감지됨
- 가격 원천 또는 AI 산출 근거가 공개 조건을 충족하지 못함

## 8. 단계별 구축 순서

### 단계 A — 현재 베타 자동운영 고정

- 업무 큐·자동 테스트·승인 패킷을 매일 실행
- GABA 한 원료, 폐쇄형 참여자, 실제 결제 없는 Shadow Pilot 운영
- 모든 수치는 시뮬레이션 또는 검증된 출처로 명시

### 단계 B — 상용 원장 연결

- PostgreSQL 스키마와 원자 트랜잭션 검증
- Object Storage 원문 보관과 SHA-256 검증
- 서명 인증, 조직 권한, 백업·복구 리허설
- readiness가 `GO`일 때만 운영 배포

### 단계 C — 제한된 실물 거래

- 초대된 구매자·공급자만 사용
- 품질·계약·정산은 H-01 및 지정 책임자의 승인
- 장애 시 자동 거래중지와 수동 복구

### 단계 D — 다원료 확장

- 원료별 Material Master·규제·보관·검수 템플릿 분리
- 조직·국가·통화·단위·언어 확장
- 원료별 위험 모델과 증빙 번들 승인
- 확장 전 회귀 시뮬레이션과 선행기술 검토

## 9. 현재 상태와 다음 결정

현재 자동 검증은 통과했지만 상용 자동운영은 아직 `NO_GO`다. 최신 전체 운영 사이클 `OPS-CYCLE-20260908215027`의 자동 실행 `AUTOPILOT-20260908215113`은 총 64건 중 64건 통과·실패 0건·3건 `SKIP`, 자동 판단 `HUMAN_REVIEW_REQUIRED`로 종료됐다. 미해결 트리거 업무 15건과 H-01 승인함 15건이 동기화되어 있으며, 브라우저에서 공급자 자격 카드·검증 조건·온보딩 입력 폼·운영 제어탑·공급자 검증 전용 큐·참여 팀 명부·Material Master 기반 `가바` 검색·AI 작업 패킷·BM특허 설명 패킷·R-01~R-07 환경 진단 표시를 확인했다. PostgreSQL 실연결·동시성·백업 리허설은 `DATABASE_URL` 미제공으로 `SKIP` 상태이며, 정규 원장 재조정은 실제 환경 증거 확보가 `TASK-2026-019`의 잔여 게이트다. 이번 사이클에는 주문 `spec_attributes`의 Material Master 일치 검증, 승인 스펙 변경 감지 회귀 테스트, 구매자 화면 진행률 오류 수정·브라우저 검증, 핵심 UI 계약 자동검사, 체결 스냅샷 재검증, BM 특허 선행기술 차이점 초안·7개 구성요소 구현 추적성, 스테이징 운영 데몬 계약·GitHub 거버넌스·상용 전환 게이트 검증, 승인함 누락 방지·과거 트리거 원문 축약 표시, PostgreSQL 승인 원장·`approval_events` 감사 이벤트 연결, 공급자 조직 자격 게이트·`/api/supplier/eligibility` 자동 확인·`/api/supplier/verification-request` H-01 승인함 접수·`/api/supplier/verification-requests/{requestId}/review` 최종 인간 검토 게이트·공급자 화면 온보딩 폼·가상 미검증 조직 fail-closed 회귀 시나리오·시뮬레이션 동적 승인함 결정·공급자 검증 운영 큐·기계 판독 가능한 TF 명부·Material Master API 기반 원료 검색·스테이징 자동 기동·GitHub 컨테이너 빌드·Compose 전체 기동 smoke test·업무 소유권과 인간 승인 게이트·AI 작업 패킷 원장·BM특허 설명 패킷 원장·상용 전환 환경 진단과 운영 화면 연결 검증을 추가로 반영했다.

운영 원장 갱신 확인: `OPS-CYCLE-20260909100855` / `AUTOPILOT-20260909100949`. 모니터링은 `OK`, 증거 66건은 모두 통과했고 3건은 PostgreSQL 환경 미설정으로 `SKIP`되었다. 자동 판단은 `HUMAN_REVIEW_REQUIRED`이며, 미해결 트리거 업무 16건과 선택 업무 1건을 포함한 H-01 승인함 17건은 자동 종결하지 않는다. 스테이징 사전점검은 비밀값을 노출하지 않고 현재 실행환경의 `PG_DRIVER`, Docker, PostgreSQL 및 Object Storage 연결정보 누락을 `BLOCKED`로 표시했다. 예약 멱등성은 동일 요청만 재사용하고 다른 요청의 예약 ID 재사용을 차단하며, 0·음수·NaN·무한대 수량과 가격 0도 차단한다. 모니터링 사고는 복구 후에도 H-01 검토 대기 기록으로 보존한다.

따라서 다음 H-01 결정은 하나다.

> PostgreSQL·Object Storage·서명 인증·백업 복구 리허설을 담당할 실운영 환경을 승인할 것인가?

승인 전까지 AI 팀은 시뮬레이션, 테스트, 문서화, 위험 탐지, 승인 패킷 작성까지만 자동으로 반복한다.
