# GABA 가격원천 승인·검증 패킷 v0.1

## 목적

`GABA-SPEC-001`의 평균 거래가격과 추세를 공개하기 전에 가격 원천의 신뢰성·단위 일치·표본 충분성·공개 권한을 검토하기 위한 패킷이다. 실제 가격 데이터가 연결되기 전에는 가격값과 AI 추세를 시장정보처럼 공개하지 않는다.

## 가격 원천 분류

| 원천 유형 | 용도 | 공개 가격지표 편입 | 필수 확인 |
|---|---|---|---|
| `COMPLETED_PHYSICAL_TRADE` | 실제 납품·검수 완료 거래 | 가능 | 체결·납품·검수·유효 증빙 |
| `SUPPLIER_QUOTE` | 현재 공급자 제안가 | 불가 | 제안 유효기간·공급자 증빙 |
| `PUBLIC_REFERENCE` | 외부 공개 참고자료 | 불가 | 발행일·단위·라이선스 |

공급자 견적과 외부 참고자료는 구매자의 판단을 돕는 참고 데이터일 뿐이며, 완료된 실물 거래 가격지표와 혼합하지 않는다.

## 원천 등록 필드

가격 원천을 등록할 때 다음 필드를 모두 제출한다. 누락·추정·수기 보정값은 승인 후보로 올리지 않는다.

```text
sourceType: COMPLETED_PHYSICAL_TRADE | SUPPLIER_QUOTE | PUBLIC_REFERENCE
sourceId:
specId: GABA-SPEC-001
supplierId:  # 원문에서는 내부 식별자, 공개 화면에서는 비식별화
price:
quantity:
quantityUnit: KG
priceUnit: KRW_PER_KG
currency: KRW
fulfilledAt:  # COMPLETED_PHYSICAL_TRADE만 필수
validUntil:   # SUPPLIER_QUOTE만 필수
publishedAt:  # PUBLIC_REFERENCE만 필수
evidenceStatus:
licenseRef:   # PUBLIC_REFERENCE만 필수
sourceDocumentRef:
sourceDocumentSha256:
```

## 품질검사 규칙

1. `specId`가 구매자가 확정한 `GABA-SPEC-001`과 일치해야 한다.
2. 가격·수량 단위와 통화는 `KRW_PER_KG / KG / KRW`로 정규화되어야 한다.
3. 완료 거래는 납품·검수 완료와 유효 증빙을 모두 가져야 한다.
4. 동일 거래 식별자는 한 번만 편입하고 재전송은 멱등 처리한다.
5. 극단값은 임의 삭제하지 않고 검토 대기 표본으로 격리한다.
6. 원천 문서·거래 상태·증빙 지문이 변경되면 기존 관측치를 재검증한다.
7. 원천의 공급자 식별정보·계약정보·개인정보는 공개 가격판에 투영하지 않는다.

## 가격값·추세 공개 조건

현재 `GABA-SPEC-001` 가격정책의 최소 기준은 다음과 같다.

- 최근 30일 이내 완료된 실물 거래 3건 이상
- 서로 다른 공급자 2곳 이상
- 모든 표본의 유효 증빙 상태 `VALID`
- 표본의 단위·통화·Spec 완전 일치
- 운영자가 가격 원천과 공개 범위를 승인

조건을 충족하지 못하면 다음 상태로 응답한다.

```text
priceStatus: INSUFFICIENT_SAMPLE | PENDING_HUMAN_APPROVAL | NOT_PUBLISHED
trendStatus: NOT_PUBLISHED
notice: 표본이 충분하지 않아 평균가격과 AI 추세를 공개하지 않습니다.
```

AI가 계산한 추세는 실제 가격을 보장하거나 매수·매도를 권유하지 않는다. 공개 시 반드시 `참고용 추세이며 보장값이 아님`을 표시한다.

## 승인 순서

1. AI-05 트렌디가 원천 메타데이터·단위·중복·표본 품질을 분석한다.
2. AI-09 콘트라가 거래 상태·증빙·Spec 일치 여부를 교차검토한다.
3. AI-11 실드가 공급자 비식별화·공개 범위·가격 조작 위험을 검토한다.
4. H-01이 원천 사용·공개 여부를 별도 승인한다.
5. 승인된 표본만 가격지표 모듈에 편입한다.
6. 기준 미달·원천 변경·증빙 만료·분쟁 발생 시 가격값과 추세 공개를 중단한다.

## 차단 조건

- 실제 원천 또는 사용 권한을 확인할 수 없음
- 완료되지 않은 주문·견적을 완료 거래 가격으로 사용하려 함
- 단위·통화·Spec이 다른 표본을 변환 근거 없이 합산하려 함
- 표본 수 또는 공급자 다양성 기준 미달
- 거래·증빙·원천 식별자가 중복되거나 서로 불일치
- 극단값을 검토 없이 삭제하거나 AI가 보정함
- H-01 공개 승인이 없음

## 현재 결정

- 상태: `PRICE_SOURCE_REQUEST_PACKET_READY`
- 현재 데이터: 시뮬레이션 전용
- 평균 거래가격: 실제 완료 거래 표본 부족으로 공개하지 않음
- AI 추세: 승인된 원천과 표본이 확보될 때까지 공개하지 않음
- 다음 작업: 실제 가격 원천 후보와 사용 권한을 수집한 뒤 H-01 검토 패킷으로 제출

