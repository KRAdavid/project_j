# Raw Material OS 배포·운영 가이드 v0.1

이 프로젝트의 베타 사이트는 Node.js 서버가 제공하는 B2B 실물 원료 거래 시뮬레이션입니다. GitHub Pages만으로는 PostgreSQL 원장, 증빙 저장소, SSE 실시간 연결을 제공할 수 없으므로 컨테이너 방식으로 배포합니다.

## 권장 배포 경로

1. GitHub Pull Request에서 `quality-gates.yml`을 통과시킵니다.
2. 승인된 커밋에 `v*` 태그를 생성합니다.
3. `container-release.yml`이 `ghcr.io/<소유자>/raw-material-os:<태그>` 이미지를 빌드·게시합니다.
4. 운영자가 관리형 PostgreSQL, S3 호환 증빙 저장소, 인증 공급자를 연결합니다.
5. `production-cutover-gate.yml`을 수동 실행해 H-01 승인과 릴리스 준비 증거를 재검증합니다.
6. 검증된 이미지 태그를 운영 인프라에 수동 배포합니다.

프로덕션 자동 배포는 의도적으로 제공하지 않습니다. 원료의 실물 납품과 증빙 검증이 연결된 서비스이므로, CI가 통과했다는 이유만으로 거래를 자동 개시하지 않습니다.

## 로컬 베타 실행

```powershell
pnpm install --frozen-lockfile
docker compose -f compose.staging.yml up --build
```

브라우저에서 `http://127.0.0.1:4173/`을 열고 `/api/health`가 200인지 확인합니다. 로컬 Docker가 없다면 시뮬레이션 메모리 모드로 다음처럼 실행할 수 있습니다.

```powershell
$env:APP_ENV = 'simulation'
$env:PERSISTENCE_MODE = 'memory'
pnpm start:beta
```

## 운영 필수 조건

프로덕션 서버는 다음 조건이 충족되지 않으면 시작하지 않습니다.

- `PERSISTENCE_MODE=postgresql`
- PostgreSQL 스키마 마이그레이션 및 백업·복구 리허설 완료
- 트랜잭션 격리와 도메인 어댑터·재조정 검증 완료
- 인증 공급자와 JWT issuer/audience/secret 설정 완료
- COA·SDS·TDS·LOT_TRACE·INVENTORY_PROOF를 보관할 증빙 저장소 연결
- 가격 데이터 소스 승인 및 모니터링·감사 연결
- H-01이 지정한 변경 티켓과 Shadow Pilot 승인 증거

운영 비밀값은 저장소 파일, Docker 이미지, GitHub 로그에 넣지 않고 호스팅 사업자의 Secret/Environment에만 보관합니다. `.env.staging.example`은 로컬 연결 예시일 뿐 실제 운영값이 아닙니다.

## 주요 엔드포인트

- `/` — 구매자·공급자 분할 베타 화면
- `/api/health` — 서버와 데이터 상태 확인
- `/api/market-board` — Material Master 기준 시장판
- `/api/events` — SSE 실시간 이벤트

## 배포 전 확인

```powershell
pnpm github:preflight
pnpm github:publication-preflight
pnpm github:staging-review
node ops/test-deployment-contract.mjs
pnpm ops:validate
```

현재 저장소는 공개 GitHub 저장소와 연결되어 있지만, 운영 브랜치 반영과 실제 호스팅 배포는 별도 승인 단계입니다.
