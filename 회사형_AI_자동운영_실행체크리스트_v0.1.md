# 회사형 AI 자동운영 실행 체크리스트

버전: v0.1  
기준일: 2026-09-08  
적용 범위: GABA 폐쇄형 베타 → 실물 원료 거래 서비스

## 1. 회사처럼 돌아간다는 의미

AI가 임의로 거래를 실행한다는 뜻이 아니다. 다음 운영 루프가 사람의 수동 지시 없이 반복되고, 위험한 실행만 H-01의 승인함으로 올라오는 상태를 뜻한다.

```text
목표·문제 감지
→ 업무 카드 자동 생성
→ 담당 AI 실행
→ 검토 AI 교차검증
→ 테스트·증거 첨부
→ H-01 승인 또는 보류
→ 안전한 조치 실행
→ KPI·감사기록 저장
→ 다음 업무 자동 생성
```

## 2. 반드시 필요한 8개 운영 장치

| 장치 | 자동화할 일 | 인간이 보유할 권한 |
|---|---|---|
| 단일 업무 큐 | 업무 생성·우선순위·담당·기한·의존성 | 목표와 우선순위 결정 |
| AI 역할 계약 | 각 AI의 입력·출력·검토자·금지행위 고정 | 역할 변경 승인 |
| 이벤트 트리거 | 문서 만료, 재고 충돌, 오류, 가격 결측 자동 감지 | 예외 처리 방향 결정 |
| 증거 중심 실행 | 테스트, 해시, 로그, 결과를 업무에 자동 첨부 | 증거의 상용 사용 승인 |
| 승인함 | 고위험 결정을 한 곳에 모음 | 거래·계약·가격 공개·결제·분쟁 종결 승인 |
| Fail-closed | 조건 불명확 시 노출 중지·읽기 전용·신규 체결 중지 | 거래 재개 승인 |
| 운영 제어탑 | 건강상태, 미해결 위험, 최신 실행, 복구상태 표시 | 계속·수정·보류·중단 결정 |
| 학습 루프 | KPI와 실패 회고로 다음 업무 자동 생성 | 기준·정책의 최종 변경 |

스테이징에서는 `npm run ops:daemon`이 이 사이클을 주기 실행한다. `--once`는 검증용 단회 실행이며, 운영 잠금·승인 게이트·fail-closed 판정은 각 사이클에서 그대로 적용된다.

승인은 자동운영 실행 ID에 종속된다. 새 실행 패킷이 생성되면 과거의 승인·보류·거절 상태를 자동 승계하지 않고, 이전 결정은 참고 정보로만 표시한 뒤 H-01의 재결정을 요구한다.

운영 사이클은 단일 잠금으로 중복 실행을 차단한다. 스케줄 실행·수동 실행·재시도가 겹치면 두 번째 실행은 기존 결과를 덮어쓰지 않고 즉시 운영 검토 대상으로 종료한다.

## 3. AI 팀의 자동 업무 분담

- AI-01 세온: 업무 큐·일일 사이클·H-01 보고 패킷 통합
- AI-02 오퍼라: 구매·공급자 응답·주문·납품 운영
- AI-03 프리즘: 초보 구매자의 원료 입력·문답 UX
- AI-04 원료나: Material Master·스펙·원료 식별자
- AI-05 트렌디: 출처 기반 가격·수급·추세 분석
- AI-06 매칭: 동일 스펙과 조건의 구매자·공급자 매칭
- AI-07 큐비: COA·SDS·TDS·로트 증빙 검증
- AI-08 로지: 재고·예약·수량 대사
- AI-09 콘트라: 물리적 거래 계약·위험·BM 특허 검토
- AI-10 아틀라스: 모듈·원장·데이터·배포 구조
- AI-11 실드: 인증·권한·보안·감사·릴리스 차단
- AI-12 리콘: 독립 검증·백업·복구·장애 재현
- AI-13 케어: 문의·클레임·분쟁 자료 정리

각 업무는 주담당 AI 1명과 검토 AI 2명을 가져야 하며, 검토 결과가 충돌하면 자동으로 `CONFLICT_REVIEW`로 올리고 실행하지 않는다.

## 4. 자동 실행 허용 범위

자동 실행 가능:

- 분석·문서 초안·코드 작성·테스트·시뮬레이션
- 유효기간이 지난 매물 숨김
- 위험 로트 격리 및 신규 체결 차단
- 장애 알림·읽기 전용 전환·대체 공급자 탐색
- 운영 알림을 중복 없이 `ops/notification-outbox.jsonl`에 기록하고 운영 제어탑에 투영
- 승인된 웹훅·활성화 플래그가 모두 있는 staging/production에서만 `ops/notification-dispatcher.mjs`가 멱등 전송하고, simulation에서는 항상 `OUTBOX_ONLY`로 유지
- H-01 승인용 의사결정 패킷 생성

H-01 승인 없이는 실행 금지:

- 실제 주문 체결·공급계약 확정
- 가격지표 외부 공개
- 결제·지급·환불·수수료 확정
- 품질 적합 판정·분쟁 종결·격리 해제
- 상용 배포·DB 전환·운영 재개

## 5. 자동 운영 주기

### 매 실행 사이클

1. 업무 큐와 이전 미해결 위험을 읽는다.
2. API·DB·SSE·증빙·재고·권한·백업 상태를 점검한다.
3. 핵심 테스트와 Shadow Pilot 시나리오를 실행한다.
4. 트리거가 있으면 중복 없이 업무와 승인 항목을 만들고, 업무 생성·인수 시각 공백도 자동 감지한다.
5. queued·working·review 업무의 SLA를 평가하고 초과 업무는 AI-01·H-01 상향보고 업무로 만든다. 담당자나 기한은 AI가 임의로 변경하지 않는다.
6. 이전 트리거가 해소되면 자동 업무를 `resolved`로 기록한다.
7. 위험도 순으로 H-01 승인함에 올리고, queued 준비 업무는 자동으로 working 인수 처리한다.
8. 이미 working/review인 업무는 반복 인수하지 않고 후속 패킷으로 이어간다.
9. 변화가 없으면 조용히 기록하고, 의미 있는 변화만 알린다.
10. 외부 알림 채널이 승인·연결되지 않은 상태에서는 발송하지 않고 `OUTBOX_ONLY`로 보류한다.

운영 데몬은 프로세스가 살아 있어도 사이클 결정이 `HUMAN_REVIEW_REQUIRED` 또는 `INCIDENT_HUMAN_REVIEW_REQUIRED`이면 `lastCycleDecision`을 기록하고 H-01 검토 상태로 표시한다. 이 상태에서는 거래·계약·결제·운영 재개를 승인하지 않으며, 프로세스 장애(`DAEMON_LAST_CYCLE_FAILED`)와 구분해 원인을 정확히 보고한다.

### 매주 H-01이 선택할 수 있는 결정

`계속` · `수정` · `보류` · `중단`

결정 근거는 스펙 확정률, 검증 매물 비율, 주문 응답시간, 체결·납품 완료율, 분쟁률, 자동 차단 정확도, 복구시간, 미해결 고위험 업무 수로 제한한다.

## 6. 현재 상태와 다음 실행

최신 운영 사이클:

- Cycle: `OPS-CYCLE-20260909100855`
- Autopilot: `AUTOPILOT-20260909100949`
- 모니터링: `OK`
- 증거 검사: 총 66건 중 66건 통과, 실패 0건, PostgreSQL 3건은 로컬 환경 미설정으로 `SKIP`
- 자동 판단: `HUMAN_REVIEW_REQUIRED`
- 상용 전환: `NO_GO`
- 자동 인수: 중복 인수 없이 기존 `working/review` 업무를 추적하며, 이번 사이클 자동 실행은 정상 종료
- 자동 트리거 대기: 16건(로컬 PostgreSQL 통합·동시성·백업·재조정 환경 증거 공백과 서버 모니터링 사고 재검토 포함)
- 트리거 큐 편입: 미해결 자동 업무를 실제 작업 큐와 동기화하고, H-01 승인함과 누락 없이 연결
- 현재 연결 범위: PostgreSQL 상태 조회·주문·수락·역제안·거절·만료·납품·검수·로트 온보딩·증빙 제출·증빙 검토·SSE 경로 연결, 정규 상태 재조정 코드와 운영 제어탑 판정 구현
- 이번 보강: 구매자가 문답으로 확정한 `spec_attributes`를 주문 원장에 복사하고, 체결 스냅샷과 재검증하여 스펙 우회를 거래 전에 차단
- 예약 멱등성 보강: 동일 예약 ID의 다른 로트·수량·스펙·납기 재사용을 `RESERVATION_ID_REUSE_MISMATCH`로 차단하고, 동일 요청만 재사용
- 예약 입력 경계 보강: 0·음수·NaN·무한대 수량을 `INVALID_RESERVATION_QUANTITY`로 차단하고 재고 불변성을 회귀 검증
- 거래 후 명령 멱등성 보강: 납품·검수·검수 기반 가격 기록의 재전송은 동일 결과만 반환하고, 다른 검수 판정은 `INSPECTION_RETRY_MISMATCH`로 차단
- 공급자 수락 멱등성 보강: 이미 체결된 주문·로트 재시도는 동일 가격·수량·스펙일 때만 기존 결과를 반환하고, 조건 변경은 `ACCEPT_RETRY_MISMATCH`로 차단
- 브라우저 상호작용 검증: GABA 6개 조건 확정 → 주문 제출 → 공급자 수신·수락 → 납품 → 검수 완료까지 성공, 콘솔 오류 0건
- 승인 동기화 검증: 미해결 자동 트리거 16건과 선택 업무 1건을 H-01 승인함 17건으로 투영하고, 결정 완료 항목의 재결정 버튼을 차단
- PostgreSQL 승인 원장 검증: 상용 경로의 승인 조회·결정·멱등성·`approval_events` 감사 기록을 정규 도메인 어댑터로 연결
- 공급자 자격 게이트 검증: 사업자 검증·활성 SUPPLIER 멤버십이 없는 조직의 매물 등록·수락·역제안을 차단
- 공급자 검증 운영 큐: 요청·H-01 진행 승인·최종 인간 증빙 검토 대기 상태를 PostgreSQL/시뮬레이션 양쪽에서 운영 제어탑에 투영하고 목록 조회 계약을 검증
- 참여 팀 원본 검증: `data/team-roster.json`의 H-01 1명·AI 가상 전문가 13명·AI 금지결정을 자동 품질 게이트와 운영 제어탑에서 공동 사용
- 업무 소유권 검증: 36개 업무의 담당 AI·검토 AI가 명부에 존재하고 모든 업무가 인간 `approval` 게이트를 갖는지 자동 검사
- AI 작업 패킷 원장: 선택 업무마다 담당 AI·검토자·증거 요약·다음 조치·금지 권한을 `ops/work-packets.jsonl`에 누적하고 H-01 승인과 분리
- BM특허 설명 패킷 원장: 7개 발명 구성요소·구현 증거·외부 선행기술 조사·변리사 검토 필요사항을 `ops/patent-disclosures.jsonl`에 누적하고 특허성 보장을 금지
- 스테이징 자동 기동 계약: `schema-migrate → app healthcheck → ops-daemon` 의존성 순서와 Dockerfile을 계약 테스트로 검증
- GitHub 컨테이너 게이트: CI에서 Compose 구성 해석과 `Dockerfile.staging` 이미지 빌드를 수행하도록 연결
- GitHub 스테이징 smoke test: Compose 전체 기동 후 `/api/health` 확인과 `down -v --remove-orphans` 정리를 자동 수행
- BM 특허 구현 추적성: 7개 발명 구성요소와 구현 파일·테스트·인간 검토 게이트의 연결을 자동 검증
- GitHub 상용 전환 게이트: 보호된 `production` 환경의 수동 실행·필수 증거·릴리스 `GO` 확인 후 자동 배포 없이 종료
- 감독자 생존 게이트: `R-05`에서 외부 모니터링 연결·heartbeat와 회사 운영 감독자 연결·heartbeat를 각각 검증하고, 누락·노후 증거는 `NO_GO`로 유지
- 상용 전환 환경 진단: 비밀값 원문을 노출하지 않고 `R-01~R-07`별 누락 환경키와 준비 상태를 API·작업 패킷에 제공
- 스테이징 사전점검: Node·`pg` 드라이버·Docker·PostgreSQL·Object Storage 연결정보를 비밀값 없이 확인하고 누락 시 `BLOCKED`로 정지
- 운영 알림 대기함: 상용 전환 불가·사고·H-01 검토·SLA 초과를 기록하고, 외부 알림 연동 전에는 자동 발송하지 않음

상용 전환을 막고 있는 항목은 `R-01` 정규 원장·재조정 환경 증거, `R-02` 조직 인증·RBAC, `R-04` 가격원천 승인, `R-05` 외부 모니터링·회사 운영 감독자 heartbeat 증거, `R-06` 백업·복구 리허설, `R-07` Shadow Pilot 인간 승인이다. PostgreSQL 도메인 명령 경로와 재조정 코드는 연결·단위 검증됐지만 실제 DB 통합·동시성·복구·재조정 증거가 확보되기 전에는 운영 원장으로 간주하지 않는다.

따라서 다음 순서는 아래와 같다.

1. 스테이징 PostgreSQL·Object Storage·인증 제공자·외부 모니터링·회사 운영 감독자를 실제로 연결하고 양쪽 heartbeat 증거를 만든다.
2. 마이그레이션, 중복 예약, DB 단절, 백업 복구, 인증 실패를 장애 주입으로 재현한다.
3. H-01이 GABA 기준 스펙·가격 공개·Shadow Pilot 범위를 승인한다.
4. 초대된 구매자·공급자만으로 폐쇄형 Shadow Pilot을 운영한다.
5. 모든 필수 체크가 통과한 뒤에만 제한적 실물 거래를 검토한다.

환경 증거가 없는 항목을 자동으로 통과시키지 않는 것이 이 시스템의 핵심 안전장치다. 현재 AI 팀은 설계·검증·시뮬레이션·위험 차단까지 자동으로 수행할 수 있지만, 상용 책임 행위는 여전히 H-01 승인 없이는 진행하지 않는다.
